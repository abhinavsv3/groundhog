# Publish to PyPI on a version tag, with trusted publishing: no token in the
# repo. One-time setup on pypi.org: project "groundhog-eval" -> Publishing ->
# add GitHub publisher (owner abhinavsv3, repo groundhog, workflow
# release.yml, environment pypi). Then:
#
#   git tag v0.2.0 && git push --tags
name: release

on:
  push:
    # Full versions only. The floating v1 tag that action.yml users pin to
    # must not trigger a PyPI publish.
    tags: ["v[0-9]+.[0-9]+.[0-9]+"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv build
      - run: |
          # The wheel must run the demo on its own: that is the whole promise.
          # Use the README's command name, groundhog-eval, so the quickstart
          # is what gets tested.
          uvx --from dist/*.whl groundhog --version
          uvx --from dist/*.whl groundhog-eval --version
          uvx --from dist/*.whl groundhog-eval demo --limit 1
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
